interface:
  display_name: "Production Template Design"
  short_description: "Design and verify production-grade product templates."
  default_prompt: "Use $production-template-design to design or refine this product as a production-grade template, implement meaningful interactions, support responsive light/dark themes, and verify the result in a real browser."

policy:
  allow_implicit_invocation: true
