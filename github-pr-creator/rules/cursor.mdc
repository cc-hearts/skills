---
description: Draft standard, publication-ready Pull Requests for both GitHub and Gitee using antdv-next templates. Confirms target base branch, auto-detects platform, extracts issues (#123 or #Ixxxx), and generates bilingual changelogs.
globs: *
alwaysApply: false
---

# GitHub & Gitee PR Creator (Cursor Rule)

When requested to create, draft, or prepare a Pull Request (PR) for GitHub or Gitee:

## 1. 目标分支与平台确认 (Branch & Platform Confirmation)
- 识别当前分支名与平台（根据 `git remote` 域名自动识别 GitHub 或 Gitee）。
- 如果未指定目标基准分支，先询问用户要 PR 合并到哪个分支（例如 `main`, `next`, `master`）。

## 2. 差异分析与 antdv-next 模版精准填充
- 比较当前分支与目标分支的代码差异。
- 自动勾选分类 `### 🤔 本次变更属于 ...`（如新功能、Bug 修复、TS 类型、组件样式、测试用例等）。
- 关联相关 Issue（GitHub 支持 `#123`，Gitee 支持 `#Ixxxx`）。
- 提炼背景与方案，生成双语变更日志（`### 📝 变更日志`）。

## 3. 输出交付
- 规范 PR 标题（Conventional Commits 格式）。
- 完整 Markdown 描述。
- GitHub 输出 `gh pr create` 命令；Gitee 输出带预选分支的直达创建链接。
