---
description: 为任意用户与场景锻造完整的设计系统 starter（tokens、AI 约定、verify 检查、介绍页）。当用户想创建、定制或批量生产设计系统，或想把自己的审美提炼成规范时使用。
globs: *
alwaysApply: false
---

# Design System Forge（设计系统锻造）

当用户想要创建、定制或批量生产一套设计系统时，按以下协议执行：

## 1. 采集参数（缺什么问什么，一次问齐）
- **领地**：系统管哪类输出——AI 文档报告 / Web 界面博客 / 幻灯片海报 / 未定。
- **执行者**：AI agent 为主 / 自己写代码为主 / 两者都要。
- **审美方向**：纸感编辑风 / 冷静极简 / 高对比现代 / 从我的作品反推（反推时收集 3-5 个对味产物，走五组二选一：暖冷 / 衬线 / 一色多色 / 方圆 / 疏密）。
- **名字**：一个有含义的词，名字要能反过来当裁判。

## 2. 推导链（顺序不可乱）
领地 → 主张 → 铁律 → token → 决策表 → 验证，每一层从上一层严格推导。
- 铁律 2~3 条，每条可判定且带理由；全部规则合计 ≤10 条。
- 每条铁律至少对应一个 verify 检查。
- 颜色的唯一事实来源是 tokens.css，其他文件禁止裸色值。
- 方法论详见 `references/methodology.md`，审美方向参考值详见 `references/presets.md`。

## 3. 生成与替换
复制 `assets/xuan-template/` 五件套（tokens.css / SKILL.md / verify.mjs / index.html / README.md）为新项目，搜索 `TODO(你)` 逐个替换：
1. 名字：目录、`<title>`、hero、导航 brand、页脚题词（题词一句话解释名字含义）。
2. 主张："我们 ____，不 ____"句式。
3. 主色三件套：`--accent` 基色 + `--accent-ink` 深一档（hover）+ `--accent-wash` 浅一档（标签底）+ `--ring` 同步换色。
4. 审美差异值：灰阶、字阶、圆角、深度按 presets 调整。
5. 决策表与反面：反面至少 4 组，每组必须带原因。
6. verify.mjs 规则必须匹配本次铁律（铁律变了规则就变）。
7. README 推导链一节用本次真实决策重写。
交付物中不出现"思路受 XX 启发"之类的致敬行。

## 4. 质检（强制，跳过不得交付）
1. `node verify.mjs <项目目录>` 通过。
2. 反向测试：临时造违规文件确认 verify 逐条拦截、退出码 1，然后删除。
3. 浏览器打不开 file: 链接——先起本地静态服务，逐屏截图自查：webfont 是否真正加载（serif 标题显示为中易宋体即为失败）、每节布局与组件观感；不满意改 tokens.css 再看。
4. 最后对照主张自查：verify 只能证明没违反铁律，不能证明好看。
